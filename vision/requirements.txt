numpy>=1.26
opencv-python>=4.10
Pillow>=10.4
pydantic>=2.9

# Inferencia de los modelos CNN propios (.onnx): número del HUD (ángulo),
# número y puntero del viento. Corren en CPU, ~1 ms. Los modelos están
# versionados en vision/models/ (no se descarga nada en runtime).
onnxruntime>=1.20
