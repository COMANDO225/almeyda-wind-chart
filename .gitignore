# --- Python ---
__pycache__/
*.py[cod]
*$py.class
.venv/
venv/
.env
*.egg-info/

# Modelos OCR descargados on-demand (~15 MB)
vision/models/*.onnx
vision/models/*.pt
vision/models/*.pth
# Excepcion: nuestro modelo custom entrenado SI se versiona (612 KB, artefacto del proyecto).
!vision/models/digit_classifier.onnx

# --- Node / Svelte / Vite ---
node_modules/
/build
/.svelte-kit
/package
.env.*
!.env.example
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# --- Rust / Tauri ---
src-tauri/target/
# Cargo.lock SÍ se commitea — somos una app, no lib (reproducibilidad)
# (no excluir Cargo.lock)

# --- Logs y debug runtime ---
tauri-dev.log
tauri-dev.log.err
*.log.err
assets/samples/_debug/

# --- Capturas privadas del usuario ---
# Las screenshots con info del juego no se publican.
# Pero el hud_reference.png anotado en assets/docs/ SÍ se commitea.
assets/samples/*.png
assets/samples/Screenshot*
!assets/samples/*.png
# Permitir el ground truth + roi config:
!assets/samples/.gitkeep
!assets/samples/ground_truth.json
!assets/samples/example.roi.json

# Dataset de entrenamiento de la CNN (privado, capturas del juego del usuario).
# Las imagenes crudas/etiquetadas se quedan en local; se versionan los
# scripts, el modelo .onnx y los labels JSON (anotaciones, son metadata chica).
assets/dataset/raw/
assets/dataset/labeled/
assets/dataset/_debug_seg/

# --- IDE ---
.vscode/*
!.vscode/extensions.json
.idea/
*.swp
.DS_Store

# --- Backups (mientras setup) ---
*.backup
*.backup.md
